<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(SolutionDirectory)Build\Shared.Overrides.targets" />

  <Target Name="BeforePrepareBuildProperties" BeforeTargets="PrepareBuildProperties">
    <PropertyGroup>
      <TempDirectory>$(TempDirectory)Dms\</TempDirectory>
      <OutputDirectory>$(OutputDirectory)Dms\</OutputDirectory>
    </PropertyGroup>
  </Target>
</Project>
